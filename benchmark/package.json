{
  "scripts": {
    "bench": "esbuild --bundle --target=es2020 --platform=node benchmark.ts | node"
  },
  "dependencies": {
    "@hms-dbmi/viv": "^0.11.0",
    "fast-csv": "^4.3.6",
    "zarr": "^0.5.1",
    "node-fetch": "^3.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.13.15"
  }
}
