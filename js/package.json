{
  "scripts": {
    "start": "run-p serve bench --race",
    "bench": "esbuild --external:zarr --bundle --target=es2020 --platform=node benchmark.ts | node",
    "serve": "docker run -p 8080:80 -v $(pwd)/data:/usr/share/nginx/html nginx"
  },
  "dependencies": {
    "@hms-dbmi/viv": "^0.11.0",
    "fast-csv": "^4.3.6",
    "node-fetch": "^3.1.0",
    "zarr": "^0.5.1"
  },
  "devDependencies": {
    "esbuild": "^0.13.15",
    "npm-run-all": "^4.1.5"
  }
}
